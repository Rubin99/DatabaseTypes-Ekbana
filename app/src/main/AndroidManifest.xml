<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.databasetypes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatabaseTypes">
        <activity android:name=".recyclerview.ItemsUpdateActivity"></activity>
        <activity android:name=".recyclerview.ItemsAddActivity" />
        <activity android:name=".recyclerview.RecyclerActivity" />
        <activity android:name=".fileio.FileActivity" />
        <activity android:name=".sharedpreferences.SecondActivity" />
        <activity android:name=".sharedpreferences.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>